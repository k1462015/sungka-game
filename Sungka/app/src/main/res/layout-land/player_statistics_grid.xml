<android.support.v7.widget.GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/detail_additional_pane"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="2"
    android:background="#455A64"
    android:paddingBottom="@dimen/player_statistics_vertical_padding"
    android:paddingEnd="@dimen/player_statistics_horizontal_padding"
    android:paddingLeft="@dimen/player_statistics_horizontal_padding"
    android:paddingRight="@dimen/player_statistics_horizontal_padding"
    android:paddingStart="@dimen/player_statistics_horizontal_padding"
    android:paddingTop="@dimen/player_statistics_vertical_padding"
    app:columnCount="2">

    <TextView
        android:id="@+id/games_played_label"
        style="@style/PlayerStatisticsTextRight"
        android:layout_height="wrap_content"
        android:text="@string/games_played"
        app:layout_columnWeight="1"
        app:layout_gravity="fill"
        app:layout_rowWeight="1" />

    <TextView
        android:id="@+id/games_played_number"
        style="@style/PlayerStatisticsTextLeft"
        app:layout_gravity="fill" />

    <TextView
        android:id="@+id/games_won_label"
        style="@style/PlayerStatisticsTextRight"
        android:text="@string/games_won"
        app:layout_gravity="fill"
        app:layout_rowWeight="1" />

    <TextView
        android:id="@+id/games_won_number"
        style="@style/PlayerStatisticsTextLeft"
        app:layout_gravity="fill" />

    <TextView
        android:id="@+id/games_lost_label"
        style="@style/PlayerStatisticsTextRight"
        android:text="@string/games_lost"
        app:layout_gravity="fill"
        app:layout_rowWeight="1" />

    <TextView
        android:id="@+id/games_lost_number"
        style="@style/PlayerStatisticsTextLeft"
        app:layout_gravity="fill" />

</android.support.v7.widget.GridLayout>

